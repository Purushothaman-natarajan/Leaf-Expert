# ──────────────────────────────────────────────────────────────────
# Leaf-Expert Backend — Python Requirements (CPU build)
# For CUDA GPU support, see requirements-gpu.txt
# ──────────────────────────────────────────────────────────────────

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-multipart>=0.0.9

# ML — PyTorch stack (CPU)
torch>=2.4.0
torchvision>=0.19.0
timm>=1.0.9

# XAI
grad-cam>=1.5.4
captum>=0.7.0
lime>=0.2.0.1

# Data / ML utils
scikit-learn>=1.5.0
matplotlib>=3.9.0
seaborn>=0.13.2
numpy>=1.26.0
pillow>=10.4.0
tqdm>=4.66.4

# Dev / testing (install with: pip install -r requirements-dev.txt)
# pytest, httpx, pytest-asyncio
